FUNCTION validatePassword(pass: STRING) RETURNS BOOLEAN
    IF pass = "MyPass"
        THEN
            RETURN FALSE
    ELSE IF pass = "MyPass?"
        THEN
            RETURN FALSE
    ELSE
        RETURN TRUE
    ENDIF
ENDFUNCTION

OUTPUT validatePassword("MyPass")